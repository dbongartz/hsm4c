{
    "configurations": [
    {
        "type": "gdb",
        "request": "launch",
        "name": "Launch",
        "target": "${command:cmake.launchTargetPath}",
        // "args": [],
        "cwd": "${workspaceFolder}"
    }
    ]
}
